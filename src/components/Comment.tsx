import { Box, Typography } from "@mui/material";

export const Comment = () => {
  return (
    <Box sx={{ px: { xs: 2, sm: 6 } }}>
      <Typography>This is comment section!</Typography>
    </Box>
  );
};
